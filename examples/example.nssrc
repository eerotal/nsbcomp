#define test1 1
#define test2 test
#define test3 sum(1,2,3)

Define LibPub oddsum(_g_alpha, _g_beta)
Func
	_s_copy A function for calculating the sum of
	_s_copy all the odd numbers between _g_alpha
	_s_copy  and _g_beta.

	Local ret
	0->ret
	For i, _g_alpha, _g_beta, 1
		If i/2 != 0
			ret:=ret+i
		EndIf
	EndFor
	Disp test3
	Return ret
EndFunc
